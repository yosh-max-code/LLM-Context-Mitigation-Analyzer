# LLM-Context-Mitigation-Analyzer
a Python tool to compare LLM performance using two different input strategies: feeding information in small chunks with context refresh versus sending large batches at once
